// import { Events } from 'discord.js'
// import { discordClient } from '@/lib/discord'

// export function authenticateDiscordLogin(token: string): Promise<string> {
//   discordClient.once(Events.ClientReady, (c) => {
//     console.log(`Ready! Logged in as ${c.user.tag}`)
//   })

//   const botAutoShortsDc = discordClient.login(token)

//   // const slashCommands = new discordClient.Colec()

//   return botAutoShortsDc
// }
